@{
  ViewData["Title"] = "Pantry";
}

<div class="row m-3">
  <div class="col-sm-12 px-2">
    <div class="mt-3 p-4 border rounded inset-shadow-sm bg-white">
      @* @await Html.PartialAsync("./Recipe/LeftSide.cshtml") *@
    </div>
  </div>
</div>
